// use advtools::itertools::Itertools;
// use advtools::input::input_string;
use advtools::input::iter_input;

fn main() {
    // let code = input_string();

    advtools::verify("1", x, y);

    advtools::verify("2", x, y);
}
